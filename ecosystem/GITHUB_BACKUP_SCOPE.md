# GitHub account backup

- Account: worthwyl2022-cloud
- Created (UTC): 2026-09-14T01:53:16Z
- Repository mirrors: `repos/`
- Gist mirrors: `gists/`
- Metadata: `metadata/`

This backup contains 19 owned repository mirrors, including Git history, branches, tags, and refs, plus account and repository metadata. No gists were found. The backup process was read-only with respect to GitHub: it did not change repository visibility, permissions, files, or account settings.

Repository directories are bare Git mirror repositories. To inspect one, use for example: `git --git-dir repos/REPOSITORY.git log --all`.

Scope note: this backup covers repository Git content, owned gists, and metadata returned by the authenticated GitHub API. GitHub-hosted account settings, organization-level data, issues/discussions metadata, Actions artifacts, releases/assets, and other service data may require a separate GitHub data export or API-specific backup if needed.
