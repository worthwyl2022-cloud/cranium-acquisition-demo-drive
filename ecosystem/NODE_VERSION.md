# Node.js runtime

This product bundle is validated on Node.js 24 LTS. Use the pinned `.nvmrc` or `.node-version` file before installing dependencies.

```bash
nvm install
nvm use
corepack enable
```
